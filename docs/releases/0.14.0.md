# 0.14.0 validation

Source commit: `07607ac`. Completed and observed 2026-09-09 at 16:00:33 UTC.

- `cargo xtask check`: formatting, warning-free Clippy, 377 all-features
  tests and x86_64/aarch64 Linux musl all-target compile checks passed.
- `cargo test --workspace --all-features --release --locked`: 377 tests passed.
- `cargo xtask deny`: advisories, bans, licenses and sources passed.
- `cargo check -p flowsdn-identity --no-default-features`: passed.
- The external package set remains unchanged at 89 versions. The optional
  filter adds a direct dependency on the already locked regex syntax parser.
- Counts: script 92, reconciler 72, config 51, ABI 45, identity 40, table 30,
  loader 12, harvester 9, health 7, fence 7, selector 7 and version 5.

New regressions cover CIDR masking and every prefix length, default/CLI/file
filter semantics, ASCII shorthand classes and word boundaries with Unicode
literals/case folding, fixed tunnel bytes and invalid fields, frozen tail
slots, inventory errors and checked scratch layout arithmetic.

Cross-review caught the Rust/RE2 shorthand-class difference before release.
Parsed-syntax normalization corrected it without disabling Unicode literals
or properties. A scratch remainder operation was made explicitly checked for
the workspace lint. Shifted configuration provenance references were updated;
no default values changed.

This source-only foundation prerelease provides individual wire codecs and
pure loader planning. It does not include packet/option-chain parsing, live map
or verifier validation, arm64 runtime tests, identity allocation, Kubernetes
label synthesis, program attachment or an operational networking agent.
Issue #55 settles the encoding policy, not mixed-cluster forwarding evidence.
Cleanup and publication are recorded separately in the velocity ledger.
