# 0.13.0 validation

Source commit: `3e5ba2a`. Completed and observed 2026-09-09 at 13:57:27 UTC.

- `cargo xtask check`: formatting, warning-free Clippy, 341 all-features tests,
  and x86_64/aarch64 Linux musl all-target compile checks passed.
- `cargo test --workspace --all-features --release --locked`: 341 tests passed.
- `cargo xtask deny`: advisories, bans, licenses and sources passed.
- The locked external dependency graph remains unchanged at 89 versions.
- The Rust BPF harvester passed nine tests and matched the pinned reference:
  142 translation units and 625 cases. Generating a new inventory and checking
  it again also passed. All 23 formerly unresolved entrypoints are classified.
- Identity tests cover 16 cases: seven label tests and nine numeric tests.
  Numeric coverage includes all cluster-range boundaries in both encodings,
  independent bytes, scope rejection, aggregation and remote validation.

A configuration provenance regression caught two line references displaced by
new spec sections. The references were corrected; no defaults changed.
Cross-review clarified that audited harvester guards check syntactic evidence,
not arbitrary control-flow drift. The CLI requires the pinned, clean reference.

This is a source-only foundation prerelease. Identity allocation, Kubernetes
label synthesis/filtering, selector matching, live maps, privileged verifier
checks, arm64 runtime tests and an operational networking agent remain absent.
No harvested networking assertions execute. Cleanup and publication timestamps
are tracked separately in the velocity ledger.
