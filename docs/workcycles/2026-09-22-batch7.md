# Remaining-backlog implementation cycle — 2026-09-22

Baseline: `3090e9e`, 40 open issues. Three agents implemented disjoint areas and
cross-reviewed changes while the coordinator serialized Git mutations and Linux
builds. Every source checkpoint travelled through GitHub. No networking release
or completed milestone is claimed.

## Delivered and verified

- Persistent agent forwarding: same pinned map across restart, IPv4/IPv6 during
  downtime, rejection of swapped foreign-interface pins, recovery from durable
  intent before BPF publication, offline deletion replay and complete cleanup.
  Mandatory nftables FORWARD drops prevent ordinary routing from masking BPF
  absence. Explicit TCX query waits account for asynchronous kernel link release.
  Cross-review fixed duplicate-interface rollback detaching another endpoint.
- AF_PACKET: 64/128/1500-byte peer injection reaches target TCX ingress with
  pass/drop/recovery; target-side send bypasses its own ingress. Capture, map
  observation and detach are checked independently.
- Globals/XDP: default17 and patched305419896 observed from `.rodata.config`;
  malformed overrides rejected. XDP total9000/linear3520 proves non-linear
  execution, and the load syscall carries BPF_F_XDP_HAS_FRAGS. Ordinary XDP also
  accepts jumbo test input on this kernel. kfunc relocation fails UnknownFunction;
  no destructive kfunc was executed and #3 remains open.
- Socket strategy: connect4/connect6 test-run returns errno524 on Linux6.17.1.
  Real cgroup attachment instead passes both destination rewrites, unmatched
  preservation, TCP payloads and cleanup. Zero link-create flags are required;
  Aya AllowMultiple produced EINVAL. This verifies the fixture, not service LB.
- Envoy: exact revision766ccfb37260a43e9d228837aa84ce3faf9f64e7 passes default,
  explicit-v2, sentinel-name and unknown-field controls. Binary SHA-256
  96a427228ab8ef043bd7be161b1de5e1e68fc1e1cf0401a0cf1aba367f711c52.
  Probe runs without capabilities; this image refuses privileged execution.
- Maglev/CIDR: all mapped upstream unit categories pass; test-port-plan separates
  measured source/data LOC from provisional whole-project estimates.
- BGP: frame/OPEN/UPDATE encoders, FSM/timers, collision ownership, GR/RIB bounds
  and notification-data regressions pass focused tests. Socket speaker and peer
  interoperability remain milestone work.
- Policy: all27 reference seeds plus deterministic mutations, generated frontend
  scenarios, independent ABI scan/index, auth/aggregate checks and conservative
  pruning are implemented. The complete Rule-to-LPM compiler remains open.
- Azure: thirteen request plans and restartable LRO state pass synthetic tests;
  malformed response types retain ownership. Generated SDK polling defects
  support application-owned REST polling; transport/live credentials remain work.
- WireGuard: fragmented large AllowedIPs round trips and caller-controlled flags
  pass; shared no_std encryption decisions map ten upstream dead-file scenarios.
  Those decisions are not complete packet/map ports; #266 remains open.
- Legacy fixtures: 100 source records /68 unique files verified byte-for-byte
  against pinned v1.16.0/v1.17.0 Git objects. Runtime adapters are not executed.
- Dependencies: fastetcd53 storage+20server tests; rustkube126apiserver+58machinery
  tests pass. The audit records missing rustkube capabilities; unit passes do not
  certify Required HTTP conformance rows.
- Rocky10: booted official image supplies all73 configured symbols; exact image
  checksum/kernel/config comparison recorded and stormcos README updated in
  `e516829`. This does not verify arm64 or physical NIC support.
- Seccomp:25 selected execs,60 observed syscall names, explicit unattributed
  counts. Derived amd64 profile passes the full agent fixture under enforcement;
  effective state confirms NoNewPrivs1/Seccomp2/one filter. Attribution and limits
  are in deploy/seccomp/PROVENANCE.md; no raw trace is committed.

## Acceptance boundaries

Remaining work includes the kfunc loader path, complete policy compiler, full
NodePort/verifier and performance measurements, physical arm64 and complete
kernel rows, live cloud capture, production networking/controllers/observers,
WireGuard packet adapters, and upstream HTTPS-defect coordination. The local
HTTPS fix and synthetic golden are present; no public upstream report was filed.
Issues291–294 remain open. No release is made merely to report issue counts.

Final workspace/cross-target checks, CI runs, closure totals, usage and cleanup
are recorded below after completion.
