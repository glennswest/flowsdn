# Changelog

## [Unreleased]

### 2026-09-07
- **chore:** Project created. Name check (GitHub, crates.io, npm, PyPI, .com/.io/.net/.org) clear.
- **docs:** Licensing analysis and clean-room protocol.
- **docs:** Scope decision (ADR-0001): full Cilium feature scope, boundary compatibility.
- **docs:** Reference pinned to Cilium v1.20.1 (7d68cfb394); inventory split into 15 areas.
- **docs:** ADR-0002 everything in Rust including BPF programs (aya-ebpf), no C. ADR-0003 no iptables; small nftables residual over netlink.
- **docs:** ADR-0004 no Hive/StateDB; explicit composition + `flowsdn-table` crate. Inventory roll-up drafted (12/15 areas) with build order.
- **docs:** Inventory complete: 15 areas, ~12.7k lines. Roll-up finalized (full-scope estimate 200–260k Rust lines).
- **docs:** Specs 00 foundation, 03 identity/ipcache, 04 conntrack/NAT. Kernel requirements roll-up: stormcos 6.12 line, general minimum 6.6, CONFIG fragment, verifier-risk analysis, test matrix.
- **docs:** Spec 01 BPF map ABI + loader. Spec wave 2 launched (05–10).
- **docs:** Spec 02 datapath programs (milestones M1/M2/M3, mark/VNI wire contract, 141-file test checklist). Spec wave 1 complete.
- **docs:** Spec wave 2 complete: 05 service load balancing, 06 policy engine, 07 IPAM, 08 endpoint + agent API, 09 CNI plugin, 10 node routing + nftables residual.
- **docs:** Spec 12 operator — process model and runtime provider selection, leader election (fatal-on-lost-leadership kept, with rationale), CRD registration and the semver "update only if older" rule, CEP GC, CES controller (default + slim modes, bin packing, dynamic rate limits, mode migration), CiliumNode GC, node taint/condition protocol, unmanaged pods, CNP validator, kvstore duties, secret sync, operator API/metrics/RBAC, full flag table, failure modes, test plan, Rust design, 10 open decisions.
- **docs:** Spec 15 BGP control plane + own minimal Rust speaker: five CiliumBGP* CRDs, operator cluster→node compilation with router-ID pool, seven agent reconcilers in priority order, export policy model (default reject, first match wins), advertisement semantics incl. ETP/ITP, aggregation, VIP refcounting and loadBalancerClass gate, RFC subset (4271/4760/6793/2918/4724+8538/8950/1997/8092/2385), FSM + notification codes, never-import invariant, pluggable Advertiser trait with a RouterOS REST backend, 20-scenario acceptance suite plus GoBGP/RouterOS interop.
- **docs:** Spec 13 CRDs + Kubernetes client layer: catalogue of the 22 `cilium.io` CRDs (identity, served/storage versions, subresources, printer columns, schema-version label 1.33.11) with a normative vendor-the-YAML-verbatim rule and seven CI diff tests (SHA manifest, document round-trip, schema↔type diff with a known-diffs allowlist, instance round-trip, unknown-field tolerance, registration-payload golden, property test); slim type field subsets for Pod/Service/Node/Namespace/Secret/EndpointSlice/NetworkPolicy and the unknown-field rule; agent and operator watch sets with field/label selectors and zero resync; a 36-row API-server capability contract doubling as the rustkube compatibility checklist plus a `flowsdn-k8s-conformance` suite derived from it; annotation/label/taint catalogue and the default identity-label filter; client configuration, URL rotation, heartbeat and startup-vs-runtime unavailability; agent RBAC; k8s floor raised to 1.26 (DEVIATION from 1.21) with the tested range 1.33–1.36; crate `flowsdn-k8s`.
- **docs:** Spec 16 L7 enforcement + DNS proxy: agent→Envoy contract (split-mode SOTW xDS over `xds.sock`, seven services and their type URLs, version/nonce ACK-NACK arithmetic with revert-on-NACK and the restore + first-NPDS-ACK barriers, the full bootstrap flowsdn generates, per-redirect listener and filter-chain shape); every `cilium.*` message with field numbers and construction rules (NetworkPolicy, PortNetworkPolicy incl. wildcard-port pruning, PortNetworkPolicyRule precedence/pass/deny, TLSContext, HeaderMatch, BpfMetadata with the explicit `ipcache_name = cilium_ipcache_v2`, NetworkFilter, L7Policy) and the deterministic sort; redirect lifecycle (port allocation/reuse/persistence, 5-attempt create, ACK installs datapath rules, teardown with the 5 min reuse delay); CEC/CCEC schema, 10-step parse/qualify/inject/validate pipeline, service and EDS wiring, nodeSelector, Ingress/Gateway path; HTTP and DNS L7 semantics incl. the never-matching missing-secret matcher and 403-on-non-match; the DNS proxy (transparent sockets, original-dst from cmsg, matchPattern→regex, the mandatory response-hold ordering and why, cache/zombie/TTL/GC, restore format, concurrency limits, every config key); standalone DNS proxy specified and recommended to ship, with `flowsdn-dnsproxy` as a shared library behind a `PolicySource` trait; mutual auth and ztunnel deferred with their datapath/handoff contracts recorded; failure modes, metrics, access-log→Hubble mapping, 90-case test plan; crates `flowsdn-xds`/`flowsdn-dnsproxy`; explicit surface list and feasibility judgement for a future Rust replacement of Envoy (policy listeners yes, arbitrary CEC no). DEVIATIONS: external Envoy DaemonSet only (no embedded process supervision), split xDS mode only, `authentication.mode: required` rejected at validation.
- **docs:** Spec 14 transparent encryption + egress gateway + ip-masq-agent: WireGuard (`cilium_wg0` lifecycle, 32-raw-byte key file with no silent regeneration, pubkey/`0xFF` publication, peer construction with duplicate-key and endpoint-selection rules, AllowedIPs incremental diff and the all-zero dummy-peer removal workaround with the GH-33159 window, peer-GC sync fences, MTU −95 with the 1280 clamp, node encryption and the control-plane opt-out bootstrap argument, overlay-in-WireGuard, both strict modes, and a 12-item explicit gap list); IPsec (key-file grammar with the 3-field panic fixed, SPI 1..15 and the `current == (active % 15) + 1` rotation predicate, per-node-pair key derivation with 4-byte IPv4 and 36-byte boot IDs, the complete XFRM object table with exact marks/masks/priorities/ESN/replay/templates incl. the default *block* policy at priority 100 and encrypted overlay, table 200 + rule prio 1, min-SPI negotiation, three-step publication order with `[0, rotation/10)` jitter, stale reclaim, EEXIST conflict recovery replicating the kernel SA lookup, the general-vs-specific IN-state workaround, 10 limitations); node IDs and `cilium_encrypt_state` with the peer→key resolution chain; egress gateway (CEGP schema field by field, parse/validation incl. the flattened node-selector cross-product, name-ordered single selection and FNV-1a-32-mod-N multi-gateway, egress IP derivation and why `egressIP` leaves ifindex 0, map keys with static prefixes and the `0.0.0.0`/`0.0.0.1` sentinels, full-diff reconciliation, hard requirements); ip-masq-agent (empty-vs-null default distinction, directory watch, restore-from-map); 22 failure modes, reference-compatible metrics, 5-part test plan, crates `flowsdn-wireguard`/`flowsdn-ipsec`/`flowsdn-egressgw` with an honest netlink gap assessment (XFRM output-mark masks, ESN replay attribute, WireGuard peer fragmentation), 10 open decisions. DEVIATIONS: parse-then-commit key loading, abort-on-dump-error reconciliation, deterministic policy iteration, KPR validated, checked ip-masq map writes, two IPv6 XFRM selector bugs fixed, legacy v4 egress map not written, and optional health-based gateway failover + a CEGP `status` subresource (both recommended, defaulted off/additive).
- **docs:** Spec wave 3 complete: 11 Hubble/monitor, 12 operator, 13 CRDs + k8s client, 14 encryption/egress, 15 BGP, 16 L7 Envoy/DNS.
