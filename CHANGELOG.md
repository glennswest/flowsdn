# Changelog

## [Unreleased]

- Track project elapsed time, per-crate delivery windows and measured session
  token usage in docs/velocity, with sanitized samples and explicit shared-work
  attribution; require updates at task boundaries and releases.

## [0.10.0] - 2026-09-09

- Add conntrack and NAT values with lossless byte codecs and frozen layout checks.

## [0.9.0] - 2026-09-09

- Begin the map ABI foundation with explicit byte order and layout contracts.
- Resolve 33 configuration defaults from owning specifications, retaining
  provenance and leaving conflicting declarations unresolved.
- Add foreground asynchronous script execution with bounded output, explicit
  environment, deadlines, cancellation and process-group cleanup.
- Include feature-gated Rust fixtures in workspace validation.
- Add periodic reconciliation refresh with rate-limited force-write hints,
  cancellation-safe replay and completion-based scheduling.

## [0.8.0] - 2026-09-09

- Reserve separate crate names for module health and active network checking.
- Add deterministic build metadata, JSON presentation and build-info labels.
- Catalogue all 539 configuration keys with 51 unresolved defaults explicitly
  requiring validated values and provenance before complete construction.
- Add a caller-owned reconciler run loop with bounded rounds, wakeups, shutdown
  and retry/prune deadlines measured after operations complete.
- Add script file existence, permission, rename, symlink and removal commands.

## [0.7.0] - 2026-09-08

- Add module health reporting and readiness evaluation.
- Add initialization-gated prune support and runtime configuration snapshots.
- Add filesystem fixtures and generic script file commands.

## [0.6.0] - 2026-09-08

- Add dependency-aware workspace change planning and focused checks.
- Add a caller-driven reconciliation core with separate status and retries.
- Add configuration cross-key and restart immutability validation.
- Add a script engine core with explicit command and condition registries.

## [0.5.0] - 2026-09-08

- Add explicit table initialization registration and sealing with cancellable waits.
- Add configuration source adapters and lexical script variable expansion.

## [0.4.0] - 2026-09-08

- Add whole-table change streams with explicit acknowledgements, coalescing,
  bounded deletion retention and lag-triggered resynchronization.
- Add typed configuration normalization, aliases, source precedence and diagnostics.
- Add concurrency, retention, index and configuration regression tests.

## [0.3.0] - 2026-09-08

- Add indexed table snapshots, conditional writes, atomic batches and explicit row rendering.
- Replace the legacy inventory harvester with Rust and verify the complete BPF case inventory.
- Keep public documentation and command help free of internal infrastructure details.
- Retain static compatibility data in txtar, YAML, TOML and JSON.

## [0.2.0] - 2026-09-08

- Add the dependency-free flowsdn-scripttest parsing front end: exact txtar
  reconstruction, flag extraction, quoted fragments, conditions, status prefixes,
  section boundaries, background syntax and line-numbered errors.
- Reject CRLF per the spec recommendation for #210; preserve LF fixture bytes.
- Add synthetic parser regressions and a static check of all 168 harvested
  scenarios. Commands are parsed, not executed; the runtime engine is next.

## [0.1.0] - 2026-09-08

- Start the Rust workspace with pinned Rust 1.95.0, shared lints and dependency policy.
- Implement ordered startup fences with shared cached results, cancellation-safe
  waiting, watch-channel readiness and debug/release registration validation.
- Add Linux build/check tasks and compile checks for both musl architectures.
- Record the implementation/backlog assessment and GitHub-only transfer rule.
- Foundation prerelease: no agent, datapath, CNI or deployable networking yet.

### 2026-09-07
- **chore:** Project created. Name check (GitHub, crates.io, npm, PyPI, .com/.io/.net/.org) clear.
- **docs:** Licensing analysis and clean-room protocol.
- **docs:** Scope decision (ADR-0001): full Cilium feature scope, boundary compatibility.
- **docs:** Reference pinned to Cilium v1.20.1 (7d68cfb394); inventory split into 15 areas.
- **docs:** ADR-0002 everything in Rust including BPF programs (aya-ebpf), no C. ADR-0003 no iptables; small nftables residual over netlink.
- **docs:** ADR-0004 no Hive/StateDB; explicit composition + `flowsdn-table` crate. Inventory roll-up drafted (12/15 areas) with build order.
- **docs:** Inventory complete: 15 areas, ~12.7k lines. Roll-up finalized (full-scope estimate 200–260k Rust lines).
- **docs:** Specs 00 foundation, 03 identity/ipcache, 04 conntrack/NAT. Kernel requirements roll-up: stormcos 6.12 line, general minimum 6.6, CONFIG fragment, verifier-risk analysis, test matrix.
- **docs:** Spec 01 BPF map ABI + loader. Spec wave 2 launched (05–10).
- **docs:** Spec 02 datapath programs (milestones M1/M2/M3, mark/VNI wire contract, 141-file test checklist). Spec wave 1 complete.
- **docs:** Spec wave 2 complete: 05 service load balancing, 06 policy engine, 07 IPAM, 08 endpoint + agent API, 09 CNI plugin, 10 node routing + nftables residual.
- **docs:** Spec 12 operator — process model and runtime provider selection, leader election (fatal-on-lost-leadership kept, with rationale), CRD registration and the semver "update only if older" rule, CEP GC, CES controller (default + slim modes, bin packing, dynamic rate limits, mode migration), CiliumNode GC, node taint/condition protocol, unmanaged pods, CNP validator, kvstore duties, secret sync, operator API/metrics/RBAC, full flag table, failure modes, test plan, Rust design, 10 open decisions.
- **docs:** Spec 15 BGP control plane + own minimal Rust speaker: five CiliumBGP* CRDs, operator cluster→node compilation with router-ID pool, seven agent reconcilers in priority order, export policy model (default reject, first match wins), advertisement semantics incl. ETP/ITP, aggregation, VIP refcounting and loadBalancerClass gate, RFC subset (4271/4760/6793/2918/4724+8538/8950/1997/8092/2385), FSM + notification codes, never-import invariant, pluggable Advertiser trait with a RouterOS REST backend, 20-scenario acceptance suite plus GoBGP/RouterOS interop.
- **docs:** Spec 13 CRDs + Kubernetes client layer: catalogue of the 22 `cilium.io` CRDs (identity, served/storage versions, subresources, printer columns, schema-version label 1.33.11) with a normative vendor-the-YAML-verbatim rule and seven CI diff tests (SHA manifest, document round-trip, schema↔type diff with a known-diffs allowlist, instance round-trip, unknown-field tolerance, registration-payload golden, property test); slim type field subsets for Pod/Service/Node/Namespace/Secret/EndpointSlice/NetworkPolicy and the unknown-field rule; agent and operator watch sets with field/label selectors and zero resync; a 36-row API-server capability contract doubling as the rustkube compatibility checklist plus a `flowsdn-k8s-conformance` suite derived from it; annotation/label/taint catalogue and the default identity-label filter; client configuration, URL rotation, heartbeat and startup-vs-runtime unavailability; agent RBAC; k8s floor raised to 1.26 (DEVIATION from 1.21) with the tested range 1.33–1.36; crate `flowsdn-k8s`.
- **docs:** Spec 16 L7 enforcement + DNS proxy: agent→Envoy contract (split-mode SOTW xDS over `xds.sock`, seven services and their type URLs, version/nonce ACK-NACK arithmetic with revert-on-NACK and the restore + first-NPDS-ACK barriers, the full bootstrap flowsdn generates, per-redirect listener and filter-chain shape); every `cilium.*` message with field numbers and construction rules (NetworkPolicy, PortNetworkPolicy incl. wildcard-port pruning, PortNetworkPolicyRule precedence/pass/deny, TLSContext, HeaderMatch, BpfMetadata with the explicit `ipcache_name = cilium_ipcache_v2`, NetworkFilter, L7Policy) and the deterministic sort; redirect lifecycle (port allocation/reuse/persistence, 5-attempt create, ACK installs datapath rules, teardown with the 5 min reuse delay); CEC/CCEC schema, 10-step parse/qualify/inject/validate pipeline, service and EDS wiring, nodeSelector, Ingress/Gateway path; HTTP and DNS L7 semantics incl. the never-matching missing-secret matcher and 403-on-non-match; the DNS proxy (transparent sockets, original-dst from cmsg, matchPattern→regex, the mandatory response-hold ordering and why, cache/zombie/TTL/GC, restore format, concurrency limits, every config key); standalone DNS proxy specified and recommended to ship, with `flowsdn-dnsproxy` as a shared library behind a `PolicySource` trait; mutual auth and ztunnel deferred with their datapath/handoff contracts recorded; failure modes, metrics, access-log→Hubble mapping, 90-case test plan; crates `flowsdn-xds`/`flowsdn-dnsproxy`; explicit surface list and feasibility judgement for a future Rust replacement of Envoy (policy listeners yes, arbitrary CEC no). DEVIATIONS: external Envoy DaemonSet only (no embedded process supervision), split xDS mode only, `authentication.mode: required` rejected at validation.
- **docs:** Spec 14 transparent encryption + egress gateway + ip-masq-agent: WireGuard (`cilium_wg0` lifecycle, 32-raw-byte key file with no silent regeneration, pubkey/`0xFF` publication, peer construction with duplicate-key and endpoint-selection rules, AllowedIPs incremental diff and the all-zero dummy-peer removal workaround with the GH-33159 window, peer-GC sync fences, MTU −95 with the 1280 clamp, node encryption and the control-plane opt-out bootstrap argument, overlay-in-WireGuard, both strict modes, and a 12-item explicit gap list); IPsec (key-file grammar with the 3-field panic fixed, SPI 1..15 and the `current == (active % 15) + 1` rotation predicate, per-node-pair key derivation with 4-byte IPv4 and 36-byte boot IDs, the complete XFRM object table with exact marks/masks/priorities/ESN/replay/templates incl. the default *block* policy at priority 100 and encrypted overlay, table 200 + rule prio 1, min-SPI negotiation, three-step publication order with `[0, rotation/10)` jitter, stale reclaim, EEXIST conflict recovery replicating the kernel SA lookup, the general-vs-specific IN-state workaround, 10 limitations); node IDs and `cilium_encrypt_state` with the peer→key resolution chain; egress gateway (CEGP schema field by field, parse/validation incl. the flattened node-selector cross-product, name-ordered single selection and FNV-1a-32-mod-N multi-gateway, egress IP derivation and why `egressIP` leaves ifindex 0, map keys with static prefixes and the `0.0.0.0`/`0.0.0.1` sentinels, full-diff reconciliation, hard requirements); ip-masq-agent (empty-vs-null default distinction, directory watch, restore-from-map); 22 failure modes, reference-compatible metrics, 5-part test plan, crates `flowsdn-wireguard`/`flowsdn-ipsec`/`flowsdn-egressgw` with an honest netlink gap assessment (XFRM output-mark masks, ESN replay attribute, WireGuard peer fragmentation), 10 open decisions. DEVIATIONS: parse-then-commit key loading, abort-on-dump-error reconciliation, deterministic policy iteration, KPR validated, checked ip-masq map writes, two IPv6 XFRM selector bugs fixed, legacy v4 egress map not written, and optional health-based gateway failover + a CEGP `status` subresource (both recommended, defaulted off/additive).
- **docs:** Spec wave 3 complete: 11 Hubble/monitor, 12 operator, 13 CRDs + k8s client, 14 encryption/egress, 15 BGP, 16 L7 Envoy/DNS.
- **test:** Harvested the upstream txtar script-test corpus verbatim (ADR-0005): 168 `.txtar` scenario files and 1444 embedded fixtures from 29 reference `testdata` directories, copied byte-identical into `tests/scripttest/corpus/<area>/` across 29 areas (loadbalancer 51, bgp 20, redirectpolicy 12, envoyconfig 12, clustermesh 9, policy/k8sclient/datapath-linux/route-reconciler 5 each, …). Every area carries a `PROVENANCE` file (reference path, commit `7d68cfb394`, Apache-2.0, copy date 2026-09-07, unmodified-copy statement); `tests/scripttest/corpus/README.md` indexes the corpus with per-area file counts and the grouped 118-command surface; `NOTICE` gained the matching attribution block.
- **docs:** Spec 17 scripttest harness (`flowsdn-scripttest`) — the txtar container format (comment-as-script, `-- name --` archive, `#!` flag line, materialization and escape rules); the script language (tokenizing and `''` quoting, the `!`/`?`/`*`/`!*` status prefixes and their exact status-check table, `[cond]`/`[!cond]`/`[prefix:suffix]` conditions, variable expansion and regexp-argument quoting, background `&` + `wait`, stdout/stderr buffers, `stop`/`skip`) with the load-bearing retry semantics spelled out: **`*` re-runs the whole `#`-delimited section, in order, not just the marked command**, on a 100 ms→500 ms doubling backoff bounded only by the script timeout; the complete command registry derived mechanically from every command line in the corpus — all 118 names in 14 groups (generic, `hive/*`, `db/*`, `k8s/*`, `lb/*`, `test/*`, netlink, reconciler drivers, `bgp/*`+`gobgp/*`, policy/identity/endpoint, `kvstore/*`, `ipam/*`, observability, misc) with arguments, flags, semantics, corpus invocation counts and the flowsdn subsystem each must drive; the `.table` column format for `db/cmp` (header-offset field splitting, case-insensitive subset columns, positional in-order row matching, `--grep`, tab mode, 5 s watch-driven internal retry) and the `lb/maps-dump` line format (SVC/BE/REV/AFF/SRCRANGE/MAGLEV, address and flag rendering, Maglev count-descending compaction); the ADR-0004 replacement for the reference's hive — an explicit per-area `Fixture` of components, tables, fences and fakes, with `#!` flags routed through the spec-00 config registry and `hive/recreate` preserving fake-k8s and BPF-map state while discarding all tables; the six fakes and their required fidelity (fake k8s with deterministic UIDs and strict/loose concurrency, in-memory *and* real-but-unpinned BPF maps with a differential test, seeded fault injection defaulting to 0.1, per-script network namespaces on a pinned thread, fake Envoy, in-memory kvstore); the ADR-0005 expected-divergence mechanism as a per-area `DIVERGENCES.toml` with XFAIL/XPASS/SKIP/partial semantics and a mandatory ADR reference; parallelism, isolation, temp dirs, timeouts and a readable failure report; the porting order against `docs/inventory/README.md` (90 of 168 files runnable at the load-balancer milestone); a harness test plan including a static "every command is registered" gate that pays off long before the subsystems exist; Rust design (three crates, `Cmd` trait, synchronous engine over `block_on`, write-our-own txtar and `.table`, `similar` for diffs) and 10 open decisions — headed by adding a `TableRender` trait to spec 00's `flowsdn-table`, which the 478 harvested `.table` files require. DEVIATIONS: no GoBGP (a Rust test peer reusing spec 15's speaker), `hive` with no arguments prints the fixture graph, `[linux]`/`[amd64]` accepted alongside the reference's `GOOS:`/`GOARCH:` spellings.
- **docs:** Spec 00 amended with the `TableRender` trait (resolves spec 17 open decision 12.1) — 478 harvested `.table` files pin column order and cell formatting, so rendering is a first-class contract, not a test-only concern.
- **docs:** Spec 19 end-to-end connectivity suite and CI matrix (`flowsdn-connectivity`, the fourth ADR-0005 harness) — the topology the suite builds (three namespaces; `client`/`client2`/`client3`, `echo-same-node`/`echo-other-node`/`echo-other-ns`, `host-netns` and an agent-free `host-netns-non-flowsdn` DaemonSet standing in for `reserved:world`; ClusterIP, headless, NodePort ×2 `externalTrafficPolicy`, LoadBalancer, session-affinity and dual-stack Services, with node placement pinned by resolved `nodeName` rather than affinity so a same-node test cannot silently become a cross-node one); the full connectivity matrix (pod↔pod same/cross node/namespace, pod→own service hairpin, every service type, pod↔host and remote-node, world→NodePort, DNS and FQDN, kube-apiserver, plus IPv6 twins and NDP/extension-header rows) with the flowsdn subsystem each row proves and three negative baseline rows that abort the run if the probe agent is lying; feature-gated scenarios for policy (L3/L4, KNP, entities, deny-beats-allow, audit, churn-under-load), L7 HTTP and DNS incl. the response-hold ordering race, egress-gateway source-IP assertion, ETP Local/Cluster incl. the no-local-backend half and `healthCheckNodePort`, session affinity, encryption (WireGuard and IPsec) with a three-part on-the-wire proof — negative AF_PACKET capture for a magic cookie, positive ESP/51871 protocol assertion, and agent/XFRM state — host firewall, BGP advertisement *reachability*, and ClusterMesh; **Hubble flow assertions as a first-class mechanism** (per-scenario `GetFlows` streams with server-side `FlowFilter`s, correlation by 5-tuple and probe header, assertions over verdict, `event_type`, `trace_observation_point`, identities, `IP.encrypted`, `Tunnel.protocol`, `IP.source_xlated` and `flow.file`, evaluated by the same `flowsdn-hubble-filter` crate the agent serves with) — the capability that turns "it connected" into "it took the expected path"; a probe agent replacing per-action `kubectl exec` with one structured `POST /probe` through the API server's pod proxy subresource; a specified sysdump (`flowsdn-connectivity sysdump`) covering agent status, every pinned `cilium_*` BPF map, up to 1M Hubble flows, kernel logs, the nftables ruleset (no `iptables-save`, ADR-0003), routes/rules/xfrm and per-scenario failure snapshots; upstream-Cilium interop in one cluster testing spec 02 §2.5's wire contract (VNI = identity ≪ 8, the `WORLD_IPV4/6→WORLD` and `HOST→REMOTE_NODE` rewrites, Geneve DSR TLV `0x014B`/`0x81`, IPv4 DSR IP option, IPsec mark/SPI encoding); upgrade/downgrade under live traffic asserting spec 01's three distinct behaviours (compatible map reused with contents preserved, incompatible agent-owned map recreated **empty with connections lost by design**, loader-owned `cilium_calls_*` never reused and pin-swapped once at commit with `BPF_LINK_UPDATE`); scale and perf smoke tests with ratio-based regression gates over committed baselines; the CI matrix (PR gate ≈10 jobs / ~25 min incl. a Rocky 10 `6.12-el10` stormcos row, nightly full 12-config × 3-kernel matrix, and an honest arm64 position — no LVH arm64 images exist, so nightly e2e runs on the Rose/stormcos arm64 nodes, an arch smoke on a GitHub arm64 runner, and QEMU aarch64 on `dev.g8.lo` for the CPU-light verifier/BPF-unit rows only); the suite's own 40-item test plan; Rust design (kube-rs `exec`/pod-proxy with no `kubectl` binary, one `flowsdn-testpod` static image replacing alpine-curl/netperf, barriers instead of sleeps with exactly one justified sleep, quarantine entries that expire); and 11 open decisions. Includes an explicit, honest list of nine things upstream's Go suite covers that this will not initially (Ingress/Gateway API, LRP, mutual auth/SPIFFE, TLS interception, netperf-comparable numbers, cloud-provider jobs).
- **test:** Surveyed the reference's Go unit-test estate and produced `docs/test-port-plan.md` — measured counts (3,080 `func Test*` in 385 packages, 275,545 test lines, 230 privileged tests in 53 packages, 212 benchmarks, 348 of 892 test files table-driven, 19 fuzz targets, 44 testdata dirs, 168 .txtar files), a per-area table of every package with >=245 test lines giving what it pins down and a disposition (port 188,730 Go test lines / harvest 31,557 / replace 22,161 / drop 14,129 / long tail 18,968), a stated effort model yielding ~102,600 lines of Rust test code, a ranked top-20 (655 test functions, 69,816 Go test lines = 25.3% of the estate) with the silent-failure mode named for each, explicit call-outs for conntrack/NAT, deny precedence, identity races, reconciler ordering, ipcache precedence, Maglev determinism, IPAM watermarks, CIDR/LPM and label canonicalisation, sequencing against the inventory build order, and 6 open questions (headed by: ADR-0003 drops 2,592 Go test lines of iptables coverage with no automatic replacement).
- **test:** Harvested the data-shaped fixtures (ADR-0005): 1,196 files in 12 areas under `tests/golden/`, byte-identical to cilium v1.20.1 (`7d68cfb394`, Apache-2.0) except one deliberate modification — the four EC private-key PEM blobs in the bugtool Envoy config-dump input replaced with a placeholder so no key material is committed. Per-area `PROVENANCE` files and `tests/golden/README.md` name the reference path, commit, license, copy date, what each fixture pins down and which flowsdn spec consumes it, plus what was deliberately not harvested and why (txtar to the sibling scripttest corpus, `bpf/tests` to the sibling BPF harness, compiled `.o`/`.c` loader fixtures, and the verified absence of any PCAP, certificate, `.table` or protobuf-text fixture in the reference).
- **test:** `tests/fuzz/SEEDS.md` — all 19 reference fuzz targets (verified against `test/fuzzing/oss-fuzz-build.sh`) with what each fuzzes, its input shape, its seed-corpus location, and a per-target carry/adapt recommendation for `cargo-fuzz`/`arbitrary`; plus six proposed flowsdn targets with no reference equivalent (CT key round-trip, Maglev determinism, LPM prefix map, ipcache metadata order-independence, identity key canonicalisation, CNP parse). The two existing corpora (27 `FuzzDistillPolicy` differential regressions, 3 `FuzzUint8` LPM seeds) copied verbatim to `tests/fuzz/corpus/` with PROVENANCE.
- **docs:** `NOTICE` gained attribution blocks for `tests/golden/**` and `tests/fuzz/corpus/**`.
- **test:** Harvested the reference BPF unit-test case inventory (ADR-0005) into `tests/bpf/CASES.toml` — 141 compiled translation units plus one upstream file that is never compiled because it is missing its `.c` extension (`encrypt_host_wireguard_tunnel`, 10 dead cases), 397 `CHECK` sections written in `.c` files, 79 more in 16 shared test headers, **625 effective cases** after per-includer header expansion (M1 279 / M2 138 / M3 208). Produced mechanically by `tools/harvest-bpf-cases.py`, which walks each `.c` with an `#ifdef`/`#elif`/`#else`-aware preprocessor so the recorded feature set, `ASSIGN_CONFIG` globals, datapath object, library modules, map-seeding groups and PKTGEN/SETUP/CHECK section list are the ones the compiler actually sees — a plain grep gets both the count and the attribution wrong, because `tc_nodeport_lb_fragments.h` picks a different entrypoint under `NORTH_SOUTH_TEST` vs `EAST_WEST_TEST` and `kpr_dsr_lb.h` picks its section prefix from `ATTACHMENT_XDP`. Each case carries the flowsdn entrypoint its SETUP stage tail-calls, resolved through up to three levels of helper/macro indirection (from_netdev 182, to_netdev 160, direct 109, from_container 63, xdp_entry 26, from_host 22, from_overlay 21, to_container 13, lxc_policy 3, to_overlay 3, unresolved 23 — never guessed). `tests/bpf/PORTED.toml` seeds the 29 non-default dispositions and `tests/bpf/PROVENANCE` records the BSD-2-Clause election, what was taken (names, flags, include-graph facts, the `pktgen.h` address book) and what was not (no C, no assertion, no expected byte, none of the scapy byte tables, none of the Apache-2.0 Go driver).
- **docs:** Spec 18 BPF test harness (`flowsdn-bpftest`, the second ADR-0005 harness) — the reference model documented in full (ELF sections `<progtype>/test/<name>/{pktgen,setup,check}`, the 3520-byte `data_in`, the four-byte status prefix the Go driver splices in front of the packet, the protobuf `SuiteResult` a BPF program hand-encodes into an 8 KiB `suite_result_map`, the `scapy_assert_map` whose failures are rendered by shelling out to Python) and then deliberately not reproduced: **all three stages move to userspace Rust** (ADR-0002), which retires ~550 lines of C macro machinery — `test_init`/`test_finish`, the `TEST()` do-while trick, the varint/protobuf writer, `trf.proto`, the 12-argument `test_log` ladder, the four `TEST_*` codes, `scapy_memcpy`/`scapy_memcmp` and the Python diff script — in favour of `assert_eq!`. Specifies the declarative `PacketBuilder` (Ethernet/VLAN/ARP/IPv4+options/IPv6+extension headers/TCP/UDP/SCTP/ICMPv4/ICMPv6/IGMP/ESP/VXLAN/Geneve+options, computed lengths and checksums including SCTP CRC32c and both pseudo-headers, deliberately-wrong checksums and lengths as first-class, fragmentation, layer-span introspection, and byte-for-byte determinism), the setup stage (typed `DatapathConfig` over `.rodata.config` — the harvest's `features` *and* `configs` both become `set_global` under ADR-0002 — the **production** loader with pinning off so the tail-call wiring under test is the shipped wiring, and typed map-seeding helpers mirroring `bpf/tests/lib/*.h`), and the assertion vocabulary (named verdicts, structured packet-field accessors, whole-packet compare with a layer-aware `FieldMask` where masking a checksummed field implicitly masks its checksum *and says so*, mark/`cb[]` decoded against spec 02 §2.1/§2.3, point/delta/counter map assertions, monitor-event matchers with volatile fields excluded by default, and a Rust layer-annotated side-by-side hexdump plus structured field diff that replaces the out-of-process scapy renderer). Also: `repeat` MUST be 1 for correctness cases and why; per-case map isolation by config-signature grouping plus asserted clearing, with randomised case order — the opposite of the reference's alphabetical determinism, chosen so order dependence surfaces; verifier statistics from both the `log_level=4` verifier log (stack depth) and `bpf_prog_info.verified_insns`, cross-checked, emitted as JSON for spec 02 §9.4's 800k-insn / 480 B gate, with a parse failure a hard error because a silently-zero budget is worse than none; the four privilege tiers and the honest statement that nothing above Tier 0 can run rootless (`bpf(2)` checks load capability against the init user namespace), so CI is root-in-a-VM and needs no cluster, no kind, no CNI and no NIC; a harness self-test plan that resolves the `ctx_in`/`ctx_out` settable-field question per kernel row before the corpus is trusted. Names the cases that are unportable or meaningless — `builtins.c` (no C builtins under ADR-0002; retargeted at `flowsdn_bpf::copy`), `_scapy_selftest.c` (no scapy), `mock_skb_metadata.c` (tests the reference harness itself), `hostfw_host_iptables.c` (no iptables under ADR-0003; half becomes an nftables-residual test) — and records the finding that the five socket-LB/`sock_destroy` files declare `CHECK("xdp", …)` and call the function under test directly with the kfunc `#define`d away, so upstream `destroy_sock_socket_lb.c` never actually exercises `bpf_sock_destroy`. Rust design notes are honest about aya: **aya 0.13 exposes no `BPF_PROG_TEST_RUN` wrapper** and the success-path verifier log may be inaccessible, so both go through a small audited raw-syscall module with upstreaming as the plan; `etherparse` is used as the parsing and checksum *oracle* rather than as the builder, because half the corpus needs packets that are wrong on purpose; `insta` snapshots the diff renderer's output but never a packet expectation; `cargo nextest` is the supported runner because network namespaces are per-thread. 10 open decisions, headed by the helper-mocking mechanism (feature-gated shim for M1, `freplace` re-evaluated at M2) — the one place where Rust has no answer to the reference's `#define fib_lookup mock_fib_lookup`.
- **docs:** Spec 22 packaging, container images, Helm chart and CI — the binary set (one multi-call `flowsdn` agent binary carrying `agent`/`build-config`/`cni`/`mount-bpffs`/`mount-cgroup`/`sysctl-fix`/`cleanup`/`wait`/`preflight`, plus deliberately-separate `flowsdn-cni`, `flowsdn-operator` ×4 cloud variants, `flowsdn-dbg`, `flowsdn-relay`, `flowsdn-dnsproxy`, `flowsdn-connectivity`), each with target triples, static-musl requirement and a CI-enforced size budget; every reference shell init container re-specified as a subcommand with its idempotency contract, including `wait --for=kube-proxy` detecting `KUBE-IPTABLES-HINT` over nftables netlink (iptables-nft rules *are* nftables objects) with an API-server fallback for legacy xtables, which turns a `privileged: true` init container into a `NET_ADMIN` one, and `mount-cgroup`/`sysctl-fix` dropping `SYS_CHROOT`+`SYS_PTRACE` by removing the `nsenter`-and-`cp`-to-host trick entirely. Scratch images with their exact file lists (~100 MiB agent vs the reference's 500–650 MiB), the full OCI + `io.flowsdn.*` label set including `bpf.objects` and both toolchain pins, and podman-only multi-arch construction (`--squash-all --timestamp`, manifest create/add/push --all); a reuse-or-rebuild judgement with a justification each for Envoy, hubble-ui, certgen, nodeinit/startup-script, SPIRE, ztunnel and clustermesh-apiserver (reused) versus hubble-relay and the operator (rebuilt). Helm chart as a compatibility contract: object names stay `cilium*` while the chart is named `flowsdn`, the values→config-key mapping is **generated** from the spec 00 key registry plus a `mapping.toml` rather than forked, and is gated by `xtask helm-diff` — render the reference chart and the flowsdn chart with the same values, diff the two `cilium-config` ConfigMaps across 45 values profiles, and fail on any difference not explained in `known-diffs.toml`. Full accepted-and-ignored tables for the iptables family (ADR-0003) and the runtime-compile/embedded-Envoy family (ADR-0002), with `bpf.masquerade=false` and `envoy.enabled=false` *rejected* rather than ignored because no fallback path exists; renames kept to zero at the values layer; new values namespaced under `flowsdn.*`. DaemonSet/Deployment shape with per-container capability sets derived from `docs/kernel-requirements.md` §4.5 (agent drops `SYS_MODULE`, `SYS_RESOURCE`, `DAC_OVERRIDE`, `FOWNER`, `SETGID`, `SETUID`, `SYSLOG`, `KILL`) and the complete hostPath table with a still-needed column (three dropped: `/proc`, `/lib/modules`, `/run/xtables.lock`; one added: `/etc/sysctl.d`). Honest upgrade assessment: CRDs, identities, the ConfigMap and pinned map layouts are shared, but per-endpoint state, program/link pins and half-migrated conntrack make in-place Cilium→flowsdn upgrade unsupported — the supported path is a per-node cordon/drain/`cleanup --all-state`/reinstall rolling migration, which works precisely because mixed Cilium/flowsdn clusters are already a spec 02 §9.5 acceptance test. Build system: the full Cargo workspace with every crate named across specs 00–22, purpose and dependencies; two toolchains (workspace stable, an excluded `flowsdn-bpf` package on a pinned nightly with `-Z build-std=core` and a pinned `bpf-linker`); BPF objects built by `xtask`, hashed into a committed `bpf-objects.lock` and embedded with `include_bytes_aligned!`; `cross` on dev.g8.lo with podman; reproducible builds via `SOURCE_DATE_EPOCH`, path remapping and single-layer images, verified by a nightly two-machine rebuild; `deny.toml` implementing `docs/licensing.md` mechanically and banning `openssl-sys`/`cc`/`bindgen`/`libnftnl-sys` so "no C" is enforced by the dependency resolver. Versioning with the nine locations that must match and an `xtask version check` gate; the GHCR question reconciled — the stormcos rule forbids a *pull on the start path*, not an image *reference*, so tarball release assets plus `sbregistry:5100` plus `imagePullPolicy: IfNotPresent` (or `Never` under `flowsdn.preloaded`) satisfy it exactly, with GHCR recommended as a mirror only once the repo is public. CI: 20 workflows with what each gates and its runtime budget, split between hosted runners and a self-hosted runner on dev.g8.lo that must set `CARGO_TARGET_DIR=/build/cargo/flowsdn` and `TMPDIR=/build/tmp` and run a `df` guard so a full SSD fails as "SSD root is for working trees only" instead of `rustc-LLVM ERROR: IO failure on output stream`; the {6.6, 6.12, 6.18} × {x86-64, arm64} matrix from the kernel roll-up; the spec 02 §9.4 verifier gate (fail > 800k insns or > 480 B stack, warn at +10 % vs a committed baseline, and distinguish "failed to load" — the panic-path symptom — from "too complex"); how the ADR-0005 scripttest, BPF and control-plane corpora are run; caching and a weekly `/build` GC. Developer workflow as `cargo xtask` (build, bpf, image, chart, kind, deploy, e2e, verifier, size-check, notice, version) defaulting to `--remote` on macOS with a one-line reminder. Plus build-observability (`flowsdn_build_info`, verifier and size trends, SBOMs, provenance attestations), 20 failure modes, a 40-item test plan and 10 open decisions.
- **docs:** Spec 20 ClusterMesh and the key-value store layer — a deliberately narrowed kvstore trait (13 operations; the reference's six `*IfLocked` variants collapse into one optional lock-guard argument, and user management leaves the trait entirely) with the etcd backend specified end to end: option table with defaults, per-handshake certificate reload as the only rotation mechanism, the three-layer dialer (static `cilium-host-aliases` with last-connected affinity → Kubernetes Service resolution → DNS), token-bucket rate limit with a bootstrap rate and an inflight cap, and the status checker's exact verdict table and frozen message strings (quorum probe on `cilium/.initlock`, the 2-minute heartbeat rule that is a remote peer's *only* liveness signal, 30 s/5 s intervals scaled by cluster size). Complete normative key schema — 26 rows under `cilium/` with value encoding, lease and writer, including the two canary overrides that drop `/id` and `/default`, and the two irregular `cilium/cache` shapes where kvstoremesh *inserts* a cluster segment. Identity allocation in kvstore mode behind spec 03's `IdentityBackend` trait: unleased master key, leased per-node slave key, the label-string lock path, per-cluster range from `cluster-id << shift`, and the two-round mod-revision GC (delete only after two consecutive round with no users and no modification) plus the lock GC that additionally compares lease ids. Agent side: config-directory recognition by the literal substring `endpoints:` with two fsnotify watchers and SHA-256 debouncing (a Secret projection swaps symlinks, so one watcher is never enough), the ten-step connection lifecycle, the cluster-ID interceptor pin, the watchdog, the five `kvstore-opt` keys that propagate and why `etcd.bootstrapQps` must not, ordering expressed as spec 00 fences (`clustermesh-ip-identities` waits on nodes too, because node records insert ipcache entries) with the 1-minute circuit breaker that returns *success*, the partial cache-TTL revocation (services drained, nodes/ipcache/identities kept so IPsec associations survive), the global-service merge with L4 port de-duplication, and the full affinity matrix including its deliberate terminating-backend asymmetry. `flowsdn-clustermesh-apiserver` with its six synchronizers, IP ownership arbitration (a reused IP would otherwise have the old pod's deletion erase the new pod's entry), global-namespace filtering that converts to *deletes* rather than dropping, and kvstoremesh's six reflectors with the exact four-step drain (config key first, 3-minute grace, `synced/`, then each cache prefix) and 2/4/8/16/32 s retries. Version skew as a capability table with additive-only evolution. **DEVIATIONS**: only `prefer-legacy` service mode is accepted, because at the reference tag the endpoint-slice import store has a dummy consumer and the other two modes silently strip every global service's remote backends; no etcd Auth — read scoping moves to an apiserver-side prefix front holding the reference's exact `local`/`remote` ACL ranges; a `kvstore-resync-interval` periodic relist the reference does not have. **FASTETCD** shipped as the backend replacing the etcd sidecar and its init container (4 containers → 2, scratch images, no auth bootstrap), with a 26-row normative compatibility checklist scored against fastetcd 1.2.0: 18 pass, 8 gaps of which two are blocking — watch broadcast lag drops revisions without cancelling the watch (mitigated by the periodic relist until fixed), and `ResponseHeader.cluster_id` defaults to `1` for every deployment, which makes the interceptor pin inert unless a distinct `--cluster-id` is set, now mandatory in the manifest. Also: `--auto-compaction-retention` is revisions in fastetcd and hours in etcd; no CN→user mapping, so the reference's per-cluster etcd users cannot work; Txn bypasses RBAC and the Auth service is served without its interceptor (filed, not load-bearing for flowsdn since Auth is unused). 100-item test plan keyed to the nine harvested clustermesh txtar scenarios plus four agent-side and one kvstore scenario (ADR-0005), a `flowsdn-kvstore-conformance` binary that is the acceptance gate for a fastetcd release, and a two/three-cluster e2e matrix. Crates `flowsdn-kvstore`, `flowsdn-kvstore-store`, `flowsdn-clustermesh`, `flowsdn-clustermesh-apiserver` on the `etcd-client` crate, with the rustls per-handshake client-cert resolver named as code that must be written because no crate provides it. 10 open decisions, headed by CRD-vs-kvstore identity mode and a proposed additive per-cluster instance UUID that would turn a silent cluster-name collision into a clear error.
- **docs:** Spec 21 Gateway API + Ingress — the two front ends and the shared translation model. Staging made explicit (P1–P6 prerequisites behind the agent's Envoy path per inventory 08; stage 1 Ingress dedicated-mode only as the smallest end-to-end proof of the translator, shared mode at the end of it, stage 2 Gateway API, with stage 2 gated on stage-1 goldens being clean). API surface pinned to `sigs.k8s.io/gateway-api` v1.6.1 + mcs-api v0.5.2: seven required kinds vs four optional (TCPRoute/UDPRoute/XListenerSet/ServiceImport, discovered by plural name, absence silent), controller name `io.cilium/gateway-controller` and every generated-object name/label/annotation frozen as a compatibility surface, and the conformance profile set GATEWAY-{HTTP,TLS,GRPC,TCP,UDP} + MESH-{HTTP,GRPC} with the 7-name exempt-feature list and 2 tracked skips. The intermediate model specified as a complete normative data model — `Model`/`HttpListener`/`TlsPassthroughListener`/`L4Listener`/`HttpRoute`/`Backend`/every filter/`ServiceParams`/`Telemetry` with field types, serde names and defaults, because 324 harvested ingestion goldens are YAML of exactly these types — plus the derived queries (per-port split decision, cross-protocol SNI split) the translator depends on. Translation rules end to end: hostname intersection with listener isolation and the both-wildcard more-labels-wins rule, hostname specificity ordering, the six-key route precedence sort with the X-Forwarded-Proto redirect-guard exclusion, rule-identity vs match-key aggregation (and why `source_rule` exists), path/header/query matcher selection incl. `path_separated_prefix` and the Ingress-only authority regex, cluster naming (`ns:name:port`, colons so the agent's qualification pass leaves it alone) with weights, appProtocol/h2c and BackendTLSPolicy upstream TLS, the combined-vs-per-port listener layout with route-config naming, the generated Service and its loadBalancerClass-preserving apply, the 63-char name shortener, and the two-owner L4 EndpointSlice split. Full status surface with exact reason strings and message text for GatewayClass, Gateway, Listener, ListenerSet, route parents, GAMMA Services, BackendTLSPolicy and CiliumGatewayClassConfig, including the deliberate `Accepted: True` + `ListenersNotValid` combination and the forced-empty `supportedKinds` for terminate-mode TLS. TLS: cert refs, ReferenceGrant enforcement *before* sync registration (so the sync controller cannot become a cross-namespace exfiltration primitive), the hashed synced-secret name shared with spec 12 §3.14, and passthrough. Ingress specifics: shared vs dedicated modes and the mode-flip cleanup, the full annotation table with legacy aliases, class resolution, default secret, path-type semantics. Host-network mode for both front ends incl. the L4-unsupported rejection and bind-address layout. 21 config keys with defaults, a fixed-not-configurable table, 20 failure modes, metrics headed by the status-writes-vs-skipped pair that exposes non-determinism, and a test plan consuming all 1,149 harvested fixtures (604 reconcile + 324 ingestion + 221 translation) with expected-divergence annotation rules. Recommends running the upstream Gateway API conformance suite in CI. Crate `flowsdn-gateway` (model/ingest/translate/controller, BTreeMap-only, prost via `flowsdn-envoy-proto`), 12–16k lines with the status surface as the schedule risk. DEVIATIONS: `PortUnavailable` on host-network port collision (the reference lets two listeners fight over one port), reachable CiliumGatewayClassConfig validation, `nodeipam.cilium.io` added to the default LB annotation prefixes so the compiled-in default matches Helm's, and a recommendation to fix rather than reproduce the reference's swapped header/query matcher arguments on force-HTTPS routes. 10 open decisions, headed by whether to ship Ingress at all now that Gateway API supersedes it (recommendation: yes, as the staging vehicle and the migration path) and deferring XListenerSet to a stage 3.
- **docs:** Spec 07 IPAM amended for ADR-0007 — the `F` test lane is redefined from hand-written in-memory cloud API servers to **recorded-response fakes**, and a new §9.1 states the fixture layout (`tests/cloud/<provider>/<scenario>/` with `scenario.toml`, captured by a manual `flowsdn-cloud-record` xtask, never in CI), the scrubbing contract (account IDs, ARNs and resource IDs, subscription/tenant IDs, resource-group and VMSS names, Alibaba UIDs and vSwitch/VPC IDs, public IPs, DNS names, auth/MSI/bearer headers and signatures replaced deterministically; private test-VPC IPs and CIDRs deliberately kept; a pattern scan runs *before* any fixture is read), replay **at the HTTP layer** rather than at the SDK trait so each SDK's signing, retry/backoff, pagination and error mapping stay inside the tested path, strict-mode matching that fails on both an unmatched request and an unused recorded interaction, the fourteen scenarios every provider MUST carry, and the weekly live-cloud drift check as the only credentialed job.
- **docs:** Spec 19 gap 9 (cloud-provider scenarios) **narrowed** rather than closed — ADR-0007 makes the cloud IPAM control plane a pull-request gate, while end-to-end datapath testing on EKS/AKS/GKE stays out of scope, so ENI/Azure pod interfaces, cloud LoadBalancer Services, cloud-native routing MTUs and per-endpoint policy routing remain manual-only. Stated as: flowsdn proves that it asks the cloud for the right addresses, not yet that packets flow once it has them.
- **docs:** Spec 22 amended for ADR-0006 and ADR-0007 — §3.8.1 now states the single-repository/single-workspace decision and its reasons (the `flowsdn-bpf-abi` skew hazard, cross-crate ordinary changes, cross-cutting corpora, one sealed image) and names `flowsdn-bgp-proto` and `flowsdn-scripttest` as the only two later extraction candidates, with the BPF package's workspace exclusion marked as a toolchain artefact rather than a repository boundary; §3.10 gains the `cloud-fixture-scan` and `cloud-fakes` **pull-request gates** and the weekly `cloud-drift` job, with an explicit rule that `cloud-drift` is the only job in the repository permitted to hold cloud credentials, and the stale "cloud conformance jobs are deferred" note corrected.
- **docs:** `docs/inventory/README.md` build order clarified under ADR-0006 — it is a crate dependency order inside one workspace, not a repository split or release sequence; the two extraction candidates are named against their steps.
- **docs:** Spec 10 §9.1 — the enumerated nftables test plan that closes the largest deliberate coverage loss in `docs/test-port-plan.md` (ADR-0003 drops `pkg/datapath/iptables` and `pkg/datapath/iptables/ipset`: 25 tests, 2,592 Go test lines, verified exactly against the reference). **45 cases (N1–N45)**, each marked unit or network-namespace: presence *and absence* per feature gate (tunnel v4/v6 and off, WireGuard, encryption marks, the IPsec-vs-WireGuard union recorded as a DEVIATION, static proxy rules, per-port TPROXY with the reference's own mark arithmetic as fixtures, the `enable-bpf-tproxy` path that upstream cannot test because all five of its proxy tests hardcode `haveBPFSocketAssign:false`, delivery interface, the `0xC00` host mark, ENI `0x80` ct-mark, per-pod and host no-track ports, pod-CIDR no-track, rule ordering); golden rulesets for fifteen feature combinations including an `all-features` one no reference test covers, compared as *both* a decoded netlink batch and an `nft list table` rendering after an encode→decode round trip; determinism and idempotent re-apply (an unchanged desired set produces **no netlink write at all**, stronger than upstream's "no mutating command"); transaction atomicity proved with a concurrent `GETRULE` poller that must never observe a partial set — a property upstream has no test for because its rename→reinstall→delete model has no atomicity; whole-batch rejection leaving the previous table byte-identical; module autoload; foreign-table coexistence including a live default-drop `forward` chain that verifies §3.10.6's claim that `notrack`/`tproxy` still work while an `accept` does not; teardown and non-disruptive restart; the accepted-and-ignored iptables keys emitting exactly one warning each with a bit-identical rendered table, and `enable-ipv4-masquerade` without `enable-bpf-masquerade` **rejected** rather than warned; and reconciler convergence with debounce coalescing and the 30-minute self-heal. Plus a table of the reference assertions with **no** nftables equivalent because BPF now does the job — the entire `ipset` package (684 lines, 6 tests), `TestNodeIpsetNATCmds`, both masquerade-command tests, route-source SNAT, hairpin/loopback, and `TestGetProxyPorts`' ruleset-as-proxy-port-database trick — each naming what proves the BPF path instead (spec 04 §3.12 and its `host_bpf_masq_*`/`remote_node_masquerade*` cases, spec 02 §3.19 pipeline 8, spec 16's proxy-port persistence). Records that four rules the reference emits and never tests — the `0xC00` host mark, ENI CONNMARK, the `cilium_host`/`cilium_net` forward accepts and the whole of `installStaticProxyRules` — are covered here for the first time, and that testing the ENI rule requires injecting the default-route device into `render()` rather than looking it up inside, which is now a design requirement.
- **docs:** `docs/test-port-plan.md` open question 2 marked resolved and its area-03 note updated, since the enumeration it demanded now exists.
- **docs:** The open-decision backlog is now on the issue tracker rather than only inside the documents — **272 issues filed** by a one-off harvest of every `## 12. Open decisions` section in `docs/spec/*.md`, every "Open questions" section in `docs/inventory/*.md`, the consequences of `docs/decisions/*.md`, `docs/kernel-requirements.md` §6, `docs/test-port-plan.md` §6 and `tests/bpf/CASES.toml`. Each issue carries the source document and section, the verbatim statement, the options with the trade-offs as the document gives them, the document's own recommendation and what is blocked until it is settled; each carries exactly one type label (`open-decision`, `to-verify`, `deferred`, `spec-gap`, `upstream-bug`, `blocked-on-code`) and one `area/*` label. An item recorded in several documents is filed once with every source named. `docs/open-decisions-index.md` is the index — the full table, counts by type and by area, and the ten whose resolution unblocks the most other work.
- **docs:** Re-harvested specs 20 and 21 now that both are complete — the harvest ran while they were still being written and filed placeholders #251 and #252 for them, both now closed as superseded. **18 new issues (#273–#290)**: seven from spec 20 (the `kvstore-resync-interval` default, the double-write identity modes, fastetcd peer TLS, the per-cluster instance UUID that would turn a silent cluster-name collision into an error, where the prefix-scoping front lives, filing the fastetcd conformance gaps F6/F7/F10/F14/F17 on the fastetcd repo, and the MCS-API and EndpointSlice-mirroring deferrals that §12 does not record) and eleven from spec 21 (whether to ship Ingress at all, the reference's swapped header/query matcher arguments on force-HTTPS routes, shared mode's cluster-wide CEC, Ingress class precedence, 301 vs 308, the non-standard status reason strings, `XListenerSet`, the vendored access-log format strings, where GAMMA lives, the two conformance skips and exempt-feature list that §2.4 and §9.5 require §12 to track but it does not, and amending ADR-0005 for the external Go conformance suites). Six items were already on the tracker from another document (#20, #26, #27, #28, #29, #159) and were updated in place with the new source and the recommendation the finished spec supplies, rather than duplicated. Totals now **290 issues**: 204 `open-decision`, 36 `to-verify`, 26 `spec-gap`, 13 `deferred`, 7 `upstream-bug`, 4 `blocked-on-code`; `area/gateway` 15 and `area/clustermesh` 13. The "settle these first" ten are unchanged.
- **fix:** Corrected three dangling references to `docs/spec/17-test-harness.md`; the file is `17-scripttest-harness.md`.
- **docs:** README status block replaced — the spec phase is complete, so it now carries the corpus tallies, a reading order, and what must be settled before the datapath crates are written.
